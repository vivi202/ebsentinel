[package]
name = "autoencoder"
version = "0.1.0"
edition = "2021"
description= "A simple autoencoder"

[dependencies]
burn = { version = "0.16.0", features = ["wgpu", "dataset"] }
serde = "1.0.217"
serde_bytes = "0.11.15"
